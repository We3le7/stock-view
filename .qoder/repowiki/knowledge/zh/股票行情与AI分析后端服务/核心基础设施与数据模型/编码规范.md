- 所有核心组件（数据库、Redis、安全）均通过 `get_settings()` 获取配置，确保配置源统一。
- 数据库模型继承自 `app.core.database.Base`，并使用 `func.now()` 处理时间戳默认值。
- API 响应模型统一继承自 `ResponseBase`，包含 `code` 和 `message` 字段，实现标准化返回结构。
- 异步资源（如 Redis 连接）采用全局变量配合懒加载初始化模式，并提供显式的关闭函数。