- 抽象层：`base.py` 定义 `BaseCollector` 接口，规范实时行情、列表、K线、分时及盘口数据的异步获取方法。
- 实现层：`eastmoney.py` 与 `sina.py` 分别封装对应厂商的 API 交互、反爬请求头配置及原始数据解析逻辑。
- 调度层：`manager.py` 维护采集器实例池，按预设优先级（东方财富优先）执行请求，在捕获异常或空响应时自动切换至备用源。
- 依赖方向：具体采集器依赖基类，管理器依赖所有具体采集器实现，形成典型的策略模式架构。