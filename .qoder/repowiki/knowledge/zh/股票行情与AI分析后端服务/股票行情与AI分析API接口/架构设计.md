- 采用FastAPI框架构建，按功能垂直切分为多个路由模块（stock, quote, watchlist, ai）。
- `websocket.py` 独立处理实时行情推送，通过 `ConnectionManager` 管理连接与订阅状态。
- REST接口统一返回 `{code, message, data}` 结构，错误码在业务层直接定义。
- 依赖注入用于数据库会话（`get_db`）和配置获取（`get_settings`），实现与核心基础设施的解耦。