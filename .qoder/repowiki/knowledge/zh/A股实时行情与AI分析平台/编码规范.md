- 统一响应格式：所有 API 接口返回标准化的 JSON 结构，包含 `code`、`message` 和 `data` 字段。
- 依赖注入与单例模式：核心组件如 `collector_manager` 和 `get_settings` 采用全局单例或缓存实例，确保资源复用。
- 抽象基类规范：数据采集器与 AI 适配器均继承自 ABC 抽象基类，强制实现核心业务方法以支持多态与扩展。
- 异步优先原则：所有涉及 I/O 操作（数据库、Redis、外部 API）的函数均定义为 `async def`，保持非阻塞执行。