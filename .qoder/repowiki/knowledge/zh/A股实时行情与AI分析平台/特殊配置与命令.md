- **环境配置**：复制 `.env.example` 为 `.env` 并配置数据库与 Redis 连接信息。
- **容器化部署**：使用 Docker Compose 编排 Nginx、FastAPI、Celery Worker/Beat、PostgreSQL 和 Redis 服务。
- **开发启动**：后端通过 `uvicorn app.main:app --reload` 启动，前端通过 `vite` 启动开发服务器。